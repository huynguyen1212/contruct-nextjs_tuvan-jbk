/*
 * LoginUser  Messages
 *
 * This contains all the text for the LoginUser component.
 */

import { defineMessages } from "react-intl";

export const scope = "LoginUser";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
