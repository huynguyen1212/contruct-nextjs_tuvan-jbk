/*
 * ExpertProfile  Messages
 *
 * This contains all the text for the ExpertProfile component.
 */

import { defineMessages } from "react-intl";

export const scope = "ExpertProfile";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
