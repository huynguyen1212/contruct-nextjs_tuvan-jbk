/*
 * SignupUser  Messages
 *
 * This contains all the text for the SignupUser component.
 */

import { defineMessages } from "react-intl";

export const scope = "SignupUser";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
