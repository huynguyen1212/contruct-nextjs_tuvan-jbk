/*
 * SignupPro  Messages
 *
 * This contains all the text for the SignupPro component.
 */

import { defineMessages } from "react-intl";

export const scope = "SignupPro";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
