/*
 * LoginPro  Messages
 *
 * This contains all the text for the LoginPro component.
 */

import { defineMessages } from "react-intl";

export const scope = "LoginPro";

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: "title",
  }
});
